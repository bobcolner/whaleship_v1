<%= form_for(@signup_box) do |f| %>
  <% if @signup_box.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@signup_box.errors.count, "error") %> prohibited this signup_box from being saved:</h2>

      <ul>
      <% @signup_box.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :company %><br />
    <%= f.text_field :company %>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
